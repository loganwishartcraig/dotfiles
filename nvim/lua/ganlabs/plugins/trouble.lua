return {
  opts = {
    auto_preview = true,
    action_keys = {
      close = { "<esc>" },
      cancel = { "q" },
      jump = { "o", "<tab>", "<2-leftmouse>" }, -- jump to the diagnostic or open / close
      jump_close = { "<cr>" }
    }
  }
}
