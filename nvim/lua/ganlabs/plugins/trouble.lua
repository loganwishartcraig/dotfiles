return {
  opts = {
    auto_preview = true,
    action_keys = {
      close = { "<esc>" },
      cancel = { "q" },
      jump = { "o", "<tab>", "<2-leftmouse>" }, -- jump to the diagnostic or open / close foldsjump = { "<cr>", "<tab>", "<2-leftmouse>" }, -- jump to the diagnostic or open / close foldsjump = { "<cr>", "<tab>", "<2-leftmouse>" }, -- jump to the diagnostic or open / close foldsjump = { "<cr>", "<tab>", "<2-leftmouse>" }, -- jump to the diagnostic or open / close foldsjump = { "<cr>", "<tab>", "<2-leftmouse>" }, -- jump to the diagnostic or open / close folds
      jump_close = { "<cr>" }
    }
  }
}
